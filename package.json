{
   "main": "backend/server.js",
   "scripts": {
      "start": "node backend/server.js",
      "heroku-postbuild": "npm install --prefix frontend && npm run build --prefix frontend && npm install && npm run start"
   },
   "engines": {
      "node": "v18.18.0",
      "npm": "10.2.0"
   }
}